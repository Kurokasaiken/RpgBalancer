# CDA-08-001 — Frozen Kit Migration

## Context
Phase 08-001 of Config-Driven Architecture (CDA-MASTER-001). Migrates existing frozen kits (PgCard, WorkerCard, POI, Token, panels) to the new config-driven architecture.

## Objectives
- Migrate PgCard to config-driven architecture
- Migrate WorkerCard to config-driven architecture
- Migrate POI to config-driven architecture
- Migrate Token to config-driven architecture
- Migrate panels to config-driven architecture
- Add unit tests for migrated components
- Verify migration

## Scope

### Files to Modify
- `src/ui/**/frozen/kits/PgCard.tsx` — Migrate to config-driven
- `src/ui/**/frozen/kits/WorkerCard.tsx` — Migrate to config-driven
- `src/ui/**/frozen/kits/POI.tsx` — Migrate to config-driven
- `src/ui/**/frozen/kits/Token.tsx` — Migrate to config-driven
- `src/ui/**/frozen/kits/panels/**` — Migrate to config-driven

### Out of Scope
- Modding Layer (deferred to CDA-09-001)

## Guardrails

### Invariants
- **Config-first**: All component parameters in config
- **i18n**: i18n contracts per component
- **Skin contracts**: Skin contracts per component

### Constraints
- Migration must preserve existing functionality
- Migrated components must use config-driven architecture
- Tests must cover all migrated components

## Implementation Plan

### Step 1: Migrate PgCard
Migrate PgCard to config-driven architecture:
- Extract parameters to config
- Use component runtime (CDA-01)
- Preserve existing functionality

### Step 2: Migrate WorkerCard
Migrate WorkerCard to config-driven architecture:
- Extract parameters to config
- Use component runtime (CDA-01)
- Preserve existing functionality

### Step 3: Migrate POI
Migrate POI to config-driven architecture:
- Extract parameters to config
- Use POI system (CDA-07)
- Preserve existing functionality

### Step 4: Migrate Token
Migrate Token to config-driven architecture:
- Extract parameters to config
- Use component runtime (CDA-01)
- Preserve existing functionality

### Step 5: Migrate Panels
Migrate panels to config-driven architecture:
- Extract parameters to config
- Use component runtime (CDA-01)
- Preserve existing functionality

### Step 6: Add Unit Tests
Create comprehensive tests for migrated components

### Step 7: Verify Migration
- Ensure all migrated components work correctly
- Test with realistic scenarios
- Validate config integration

## Safeguards

### Pre-Execution
- Verify `CDA-01-001` through `CDA-07-001` are marked as `Completato` in Kanban
- Run `npm run lint -- src/ui/**/frozen/kits` (120s timeout)
- Run `npm run test -- tests/unit/ui/**/frozen/kits/` (300s timeout)

### Post-Execution
- Run `npm run lint -- src/ui/**/frozen/kits` (120s timeout)
- Run `npm run test -- tests/unit/ui/**/frozen/kits/` (300s timeout)
- Run `npm run build:check` (180s timeout)
- Run `npm run kanban:lint` (30s timeout)

### Evidence Log
Create `test-results/cda-08-001-<date>.log` with:
- Lint results
- Test results (all tests passing)
- Build check output
- Kanban lint output
- Migration summary

## Dependencies
- **blocked_by**: CDA-01-001 through CDA-07-001 (all previous CDA phases)

## Execution Hint
**architectural** — This task migrates existing components to the new config-driven architecture and requires careful preservation of existing functionality.

## Notes
- Config-first: all component parameters in config
- i18n contracts per component
- Skin contracts per component
- Migration must preserve existing functionality
