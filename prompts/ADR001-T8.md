# ADR001-T8 — End-to-end replay test + trusted docs

## Context
Part of Quest Engine Reconciliation (ADR-001). Phase T8 creates end-to-end replay tests for the quest engine and updates trusted documentation (quest_engine_trusted.md and COMPONENT_MASTER_INDEX.md) to reflect the new C2 implementation. This is the final verification phase before closing ADR-001.

## Objectives
- Create end-to-end replay test for quest engine
- Update `quest_engine_trusted.md` with C2 implementation details
- Update `COMPONENT_MASTER_INDEX.md` with quest engine status
- Verify all safeguards pass before closing ADR-001

## Scope

### Files to Create
- `tests/unit/engine/questReplay.test.ts` — End-to-end replay test

### Files to Modify
- `src/docs/docs/idle_village/trusted/quest_engine_trusted.md` — Update with C2 details
- `src/docs/docs/idle_village/COMPONENT_MASTER_INDEX.md` — Update quest engine status
- `src/docs/docs/plans/quest_engine_reconciliation_plan.md` — Update changelog

### Out of Scope
- Runtime engine changes (C2 is already complete and verified)
- UI component updates (deferred to T5)
- New quest features (deferred to future work)

## Guardrails

### Invariants
- **Documentation Governance**: Update trusted doc and COMPONENT_MASTER_INDEX before closing
- **Runtime Verification**: Replay test must pass with deterministic outcomes
- **Config-first**: All quest configurations must be in config, not hardcoded
- **i18n**: Any user-facing strings must use `idleVillage` namespace

### Constraints
- Replay test must cover full quest lifecycle (start → complete → outcome)
- Trusted doc must accurately reflect C2 implementation
- COMPONENT_MASTER_INDEX must have correct status (trusted/frozen)
- All safeguards must pass before marking task complete

## Implementation Plan

### Step 1: Create Replay Test
Create `questReplay.test.ts` with:
- Test case for full quest lifecycle with seeded RNG
- Verification that replay produces identical outcomes
- Coverage of all quest event types
- Edge case tests (failures, edge conditions)

### Step 2: Update Trusted Doc
Update `quest_engine_trusted.md`:
- Document C2 quest engine architecture
- Describe RngService integration
- Document C2 config schema
- Add examples of quest definitions
- Update status to reflect C2 implementation

### Step 3: Update COMPONENT_MASTER_INDEX
Update `COMPONENT_MASTER_INDEX.md`:
- Set quest engine status to `trusted` or `frozen` as appropriate
- Add link to updated trusted doc
- Update last certified timestamp
- Document any breaking changes from C1 → C2

### Step 4: Verify Safeguards
Run all safeguards:
- `npm run lint -- src/engine/quest tests/unit/engine/`
- `npm run test -- tests/unit/engine/questReplay.test.ts`
- `npm run build:check`
- `npm run kanban:lint`

## Safeguards

### Pre-Execution
- Verify `ADR001-T2`, `ADR001-T4`, and `ADR001-T6` are marked as `Completato` in Kanban
- Run `npm run lint -- src/engine/quest tests/unit/engine/` (120s timeout)
- Run `npm run test -- tests/unit/engine/` (300s timeout)

### Post-Execution
- Run `npm run lint -- src/engine/quest tests/unit/engine/` (120s timeout)
- Run `npm run test -- tests/unit/engine/questReplay.test.ts` (300s timeout)
- Run `npm run build:check` (180s timeout)
- Run `npm run kanban:lint` (30s timeout)
- Verify trusted doc and COMPONENT_MASTER_INDEX are updated

### Evidence Log
Create `test-results/adr001-t8-<date>.log` with:
- Lint results
- Test results (replay test passing)
- Build check output
- Kanban lint output
- Trusted doc update summary
- COMPONENT_MASTER_INDEX update summary
- Final safeguard verification

## Dependencies
- **blocked_by**: ADR001-T2 (Build deterministic resolution engine), ADR001-T4 (Purge Math.random()), ADR001-T6 (Quest telemetry + outcome parity tests)

## Execution Hint
**verified** — This task touches invariants (documentation governance, runtime verification) and requires comprehensive testing and documentation updates before closing ADR-001.

## Notes
- Replay test is critical for verifying C2 engine correctness
- Trusted doc must accurately reflect C2 implementation
- COMPONENT_MASTER_INDEX update is mandatory per documentation governance
- All safeguards must pass before marking task complete
