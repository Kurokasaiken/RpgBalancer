# CDA-04-001 — Physics System (profiles + unified hook)

## Context
Phase 04-001 of Config-Driven Architecture (CDA-MASTER-001). Implements the physics system with profiles and unified hook.

## Objectives
- Create PhysicsProfile
- Create useComponentPhysics hook
- Add unit tests for physics system
- Verify physics system integration

## Scope

### Files to Create
- `src/game/physics/PhysicsProfile.ts` — Physics profile
- `src/game/physics/useComponentPhysics.ts` — Unified physics hook
- `tests/unit/game/physics/PhysicsProfile.test.ts` — Unit tests
- `tests/unit/game/physics/useComponentPhysics.test.ts` — Unit tests

### Files to Modify
- (None — standalone phase)

### Out of Scope
- Seed / Procedural Variation (deferred to CDA-05-001)

## Guardrails

### Invariants
- **Config-first**: All physics parameters in config
- **i18n**: i18n contracts per component
- **Skin contracts**: Skin contracts per component

### Constraints
- Physics system must be config-driven
- Unified hook must be reusable
- Tests must cover all physics scenarios

## Implementation Plan

### Step 1: Create PhysicsProfile
Create `PhysicsProfile.ts` with:
- Config-driven physics profiles
- Physics parameter definitions
- Profile validation

### Step 2: Create useComponentPhysics
Create `useComponentPhysics.ts` with:
- Unified physics hook for components
- Config-driven physics application
- Physics state management

### Step 3: Add Unit Tests
Create comprehensive tests for physics profile and hook

### Step 4: Verify Integration
- Ensure physics system works with component runtime
- Test with realistic physics scenarios
- Validate config integration

## Safeguards

### Pre-Execution
- Verify `CDA-01-001` is marked as `Completato` in Kanban
- Run `npm run lint -- src/game/physics` (120s timeout)
- Run `npm run test -- tests/unit/game/physics/` (300s timeout)

### Post-Execution
- Run `npm run lint -- src/game/physics` (120s timeout)
- Run `npm run test -- tests/unit/game/physics/` (300s timeout)
- Run `npm run build:check` (180s timeout)
- Run `npm run kanban:lint` (30s timeout)

### Evidence Log
Create `test-results/cda-04-001-<date>.log` with:
- Lint results
- Test results (all tests passing)
- Build check output
- Kanban lint output
- Physics system summary

## Dependencies
- **blocked_by**: CDA-01-001 (Component Runtime)

## Execution Hint
**architectural** — This task builds on component runtime and requires careful design of the physics system.

## Notes
- Config-first: all physics parameters in config
- i18n contracts per component
- Skin contracts per component
