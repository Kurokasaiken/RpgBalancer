# CDA-07-001 — POI World Map System

## Context
Phase 07-001 of Config-Driven Architecture (CDA-MASTER-001). Implements the POI world map system with config packs.

## Objectives
- Create PointOfInterestComponent
- Create config packs
- Add unit tests for POI system
- Verify POI system integration

## Scope

### Files to Create
- `src/game/poi/PointOfInterestComponent.ts` — POI component
- `src/game/poi/configPacks/**` — Config packs
- `tests/unit/game/poi/PointOfInterestComponent.test.ts` — Unit tests

### Files to Modify
- (None — standalone phase)

### Out of Scope
- Frozen Kit Migration (deferred to CDA-08-001)

## Guardrails

### Invariants
- **Config-first**: All POI parameters in config
- **i18n**: i18n contracts per component
- **Skin contracts**: Skin contracts per component

### Constraints
- POI system must integrate with CDA-01-05
- POI config must be in config packs
- Tests must cover all POI scenarios

## Implementation Plan

### Step 1: Create PointOfInterestComponent
Create `PointOfInterestComponent.ts` with:
- Config-driven POI definition
- Integration with component runtime (CDA-01)
- Integration with rendering primitives (CDA-02)
- Integration with physics system (CDA-04)
- Integration with seed system (CDA-05)

### Step 2: Create Config Packs
Create config packs with:
- POI configuration data
- Config validation
- Config composition

### Step 3: Add Unit Tests
Create comprehensive tests for POI system

### Step 4: Verify Integration
- Ensure POI system works with all subsystems
- Test with realistic POI scenarios
- Validate config integration

## Safeguards

### Pre-Execution
- Verify `CDA-01-001` through `CDA-05-001` are marked as `Completato` in Kanban
- Run `npm run lint -- src/game/poi` (120s timeout)
- Run `npm run test -- tests/unit/game/poi/` (300s timeout)

### Post-Execution
- Run `npm run lint -- src/game/poi` (120s timeout)
- Run `npm run test -- tests/unit/game/poi/` (300s timeout)
- Run `npm run build:check` (180s timeout)
- Run `npm run kanban:lint` (30s timeout)

### Evidence Log
Create `test-results/cda-07-001-<date>.log` with:
- Lint results
- Test results (all tests passing)
- Build check output
- Kanban lint output
- POI system summary

## Dependencies
- **blocked_by**: CDA-01-001 through CDA-05-001 (Component Runtime, Rendering Primitive System, Material Engine, Physics System, Seed / Procedural Variation)

## Execution Hint
**architectural** — This task integrates multiple subsystems and requires careful design of the POI world map system.

## Notes
- Config-first: all POI parameters in config
- i18n contracts per component
- Skin contracts per component
