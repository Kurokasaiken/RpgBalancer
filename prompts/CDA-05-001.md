# CDA-05-001 — Seed / Procedural Variation

## Context
Phase 05-001 of Config-Driven Architecture (CDA-MASTER-001). Implements the seed system with procedural variation and deterministic RNG.

## Objectives
- Create SeedSystem
- Create deterministic RNG
- Add unit tests for seed system
- Verify seed system integration

## Scope

### Files to Create
- `src/game/seed/SeedSystem.ts` — Seed system
- `src/game/seed/deterministicRNG.ts` — Deterministic RNG
- `tests/unit/game/seed/SeedSystem.test.ts` — Unit tests
- `tests/unit/game/seed/deterministicRNG.test.ts` — Unit tests

### Files to Modify
- (None — standalone phase)

### Out of Scope
- Village Evolution System (deferred to CDA-06-001)

## Guardrails

### Invariants
- **Config-first**: All seed parameters in config
- **Determinism**: RNG must be deterministic (same seed = same sequence)

### Constraints
- Seed system must be config-driven
- RNG must be deterministic
- Tests must cover all seed scenarios

## Implementation Plan

### Step 1: Create SeedSystem
Create `SeedSystem.ts` with:
- Config-driven seed management
- Seed persistence
- Seed validation

### Step 2: Create Deterministic RNG
Create `deterministicRNG.ts` with:
- Deterministic RNG implementation
- Seed-based random generation
- RNG validation

### Step 3: Add Unit Tests
Create comprehensive tests for seed system and RNG

### Step 4: Verify Integration
- Ensure seed system works with component runtime
- Test with realistic seed scenarios
- Validate config integration

## Safeguards

### Pre-Execution
- Verify `CDA-01-001` is marked as `Completato` in Kanban
- Run `npm run lint -- src/game/seed` (120s timeout)
- Run `npm run test -- tests/unit/game/seed/` (300s timeout)

### Post-Execution
- Run `npm run lint -- src/game/seed` (120s timeout)
- Run `npm run test -- tests/unit/game/seed/` (300s timeout)
- Run `npm run build:check` (180s timeout)
- Run `npm run kanban:lint` (30s timeout)

### Evidence Log
Create `test-results/cda-05-001-<date>.log` with:
- Lint results
- Test results (all tests passing)
- Build check output
- Kanban lint output
- Seed system summary

## Dependencies
- **blocked_by**: CDA-01-001 (Component Runtime)

## Execution Hint
**architectural** — This task builds on component runtime and requires careful design of the seed system with deterministic RNG.

## Notes
- Config-first: all seed parameters in config
- Determinism: RNG must be deterministic (same seed = same sequence)
