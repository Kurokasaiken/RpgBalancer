AGENT
GOV-007 – Migrate `TestHub` `EXTRA_PAGES` into `KIT_REGISTRY`

ISTRUZIONI AGENTE
Sei un agente Windsurf: consulta le skill `agent-execution-mandate` e `idle-village-task` prima di iniziare, segui i mandati, completa la safeguard suite ed esegui le consegne Kanban.

INVARIANTI
- `.windsurf/rules/00-project-invariants.md` (config-first, i18n, documentation, safeguards).
- `.windsurf/rules/30-frozen-kits.md` (dopo GOV-001).
- `.windsurf/rules/40-documentation-governance.md` (dopo GOV-002).
- `src/docs/docs/plans/systems_governance_alignment_plan.md` §4.A.1, §4.E.

OBIETTIVO
Ridurre `EXTRA_PAGES` in `src/ui/idleVillage/TestHub.tsx` a sole pagine deliberate non-kit, spostando ogni entry non duplicata in `src/ui/idleVillage/frozen/registry.ts` come kit `draft` con `hub` metadata.

FILE TARGET
1. `src/ui/idleVillage/TestHub.tsx` (update)
2. `src/ui/idleVillage/frozen/registry.ts` (update)
3. `src/docs/docs/plans/systems_governance_alignment_plan.md` (read, decision matrix §5.1)

OPERAZIONI DA ESEGUIRE
1. Leggi `TestHub.tsx` `EXTRA_PAGES` e `KIT_REGISTRY`.
2. Applica la decision matrix in `systems_governance_alignment_plan.md` §5.1:
   - `slot` → rimuovi da `EXTRA_PAGES` (funzionalità coperta da `slotRackKit`).
   - `roster-slot-integration` → nuovo kit `draft` `rosterSlotKit` con `hub` copiato da `EXTRA_PAGES` e `kitModule: './kits/rosterSlotKit' // TODO`.
   - `job-poi-roster` → nuovo kit `draft` `jobPoiRosterKit` con `hub` copiato e `kitModule: './kits/jobPoiRosterKit' // TODO`.
   - `job-poi-roster-time` → nuovo kit `draft` `jobPoiRosterTimeKit` con `hub` copiato e `kitModule: './kits/jobPoiRosterTimeKit' // TODO`.
   - `quest-detail-legacy` → rimuovi da `EXTRA_PAGES` (duplicato/superato da `questDetailKit`).
   - `v8-skin-sandbox` → rimuovi da `EXTRA_PAGES` (V8 deprecato).
   - `v9-skin-sandbox` → mantieni in `EXTRA_PAGES` con commento di giustificazione (visual sandbox, non kit).
   - `poi-detail` → mantieni in `EXTRA_PAGES` con commento (verification harness, non kit).
   - `poi-quest-detail-roster-integration` → mantieni in `EXTRA_PAGES` con commento (integration verification harness).
   - `poi-job-detail-roster-integration` → mantieni in `EXTRA_PAGES` con commento (integration verification harness).
3. Se un path `EXTRA_PAGES` è già coperto da `KIT_REGISTRY.hub` (stesso path), rimuovi il duplicato da `EXTRA_PAGES`.
4. Aggiorna `TestHub.tsx` rimuovendo le `EXTRA_PAGES` migrate e aggiungendo commenti di giustificazione per quelle rimanenti.
5. Non creare i moduli kit reali; usa `kitModule` placeholder con `// TODO` (sarà compito di future prompt).
6. Verifica che `TestHub` continui a funzionare: `KIT_PAGES` + `EXTRA_PAGES` filtrati deve rimanere equivalente.

OPERAZIONI VIETATE
- Non modificare il comportamento delle pagine (solo metadata/kit registration).
- Non creare trusted doc o contract per i draft in questo task.
- Non rimuovere pagine che non siano state migrate (lascia `EXTRA_PAGES` con quelle residue e un commento).
- Non modificare `src/ui/idleVillage/frozen/kits/index.ts` se non per re-esportare kit esistenti (non creare export per moduli inesistenti).

ASSUNZIONI
- `TestHub` è generato da `KIT_REGISTRY.hub`.
- `EXTRA_PAGES` contiene pagine di test mancanti di `hub` metadata.
- GOV-001, GOV-002, GOV-003, GOV-004 sono completati.

REGRESSION SAFEGUARDS
TESTS TO RUN:
  - `npm run lint -- src/ui/idleVillage/TestHub.tsx src/ui/idleVillage/frozen/registry.ts`
  - `npm run build:check`
  - `npm run kanban:lint`
STOP CONDITIONS:
  - Qualsiasi errore di build o test che indichi che `TestHub` è rotto deve essere corretto prima di chiudere.

AUTONOMIA & CHECK-IN
- Completa tutta la checklist prima di tornare.
- Non chiedere conferme salvo conflitti di kitId duplicati.

NOTE
- L'agente deve chiudere con la riga `KANBAN STATUS: GOV-007 – Completato (Evidence: test-results/gov-007-<YYYY-MM-DD>.log)`.
