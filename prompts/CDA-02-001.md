# CDA-02-001 — Rendering Primitive System

## Context
Phase 02-001 of Config-Driven Architecture (CDA-MASTER-001). Implements the rendering primitive system with materials.

## Objectives
- Create rendering primitives
- Create material system
- Add unit tests for rendering primitives
- Verify rendering primitive integration

## Scope

### Files to Create
- `src/rendering/primitives/**` — Rendering primitives
- `src/rendering/materials/**` — Material system
- `tests/unit/rendering/primitives/*.test.ts` — Unit tests
- `tests/unit/rendering/materials/*.test.ts` — Unit tests

### Files to Modify
- (None — standalone phase)

### Out of Scope
- Material Engine (deferred to CDA-03-001)

## Guardrails

### Invariants
- **Config-first**: All rendering parameters in config
- **i18n**: i18n contracts per component
- **Skin contracts**: Skin contracts per component

### Constraints
- Rendering primitives must be config-driven
- Material system must be reusable
- Tests must cover all rendering scenarios

## Implementation Plan

### Step 1: Create Rendering Primitives
Create rendering primitives with:
- Config-driven primitive definitions
- Reusable primitive components
- Material integration

### Step 2: Create Material System
Create material system with:
- Config-driven material definitions
- Material composition
- Material validation

### Step 3: Add Unit Tests
Create comprehensive tests for rendering primitives and materials

### Step 4: Verify Integration
- Ensure rendering primitives work with component runtime
- Test with realistic rendering scenarios
- Validate config integration

## Safeguards

### Pre-Execution
- Verify `CDA-01-001` is marked as `Completato` in Kanban
- Run `npm run lint -- src/rendering` (120s timeout)
- Run `npm run test -- tests/unit/rendering/` (300s timeout)

### Post-Execution
- Run `npm run lint -- src/rendering` (120s timeout)
- Run `npm run test -- tests/unit/rendering/` (300s timeout)
- Run `npm run build:check` (180s timeout)
- Run `npm run kanban:lint` (30s timeout)

### Evidence Log
Create `test-results/cda-02-001-<date>.log` with:
- Lint results
- Test results (all tests passing)
- Build check output
- Kanban lint output
- Rendering primitive summary

## Dependencies
- **blocked_by**: CDA-01-001 (Component Runtime)

## Execution Hint
**architectural** — This task builds on the component runtime and requires careful design of the rendering primitive system.

## Notes
- Config-first: all rendering parameters in config
- i18n contracts per component
- Skin contracts per component
