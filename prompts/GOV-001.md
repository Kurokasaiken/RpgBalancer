AGENT
GOV-001 – Add Frozen Kits rule to `.windsurf/rules/`

ISTRUZIONI AGENTE
Sei un agente Windsurf: consulta le skill `agent-execution-mandate` e `idle-village-task` prima di iniziare, segui i mandati, completa la safeguard suite ed esegui le consegne Kanban.

INVARIANTI
- `.windsurf/rules/00-project-invariants.md` (persistence, config-first, i18n, documentation, Node/tooling, safeguards, shared baseline).
- `.windsurf/rules/10-ui-invariants.md` (skin, i18n, Gilded Observatory theme, dnd-kit).
- `src/docs/docs/plans/systems_governance_alignment_plan.md` §4.A.1.

OBIETTIVO
Registrare il sistema **Frozen Kits** nel layer condiviso `.windsurf/rules/` in modo che ogni agent futuro sia obbligato a trattare `src/ui/idleVillage/frozen/**` come superficie canonica per i componenti Idle Village.

FILE TARGET
1. `.windsurf/rules/30-frozen-kits.md` (new)
2. `.windsurf/rules/00-project-invariants.md` (update §"This layer is the shared baseline")

OPERAZIONI DA ESEGUIRE
1. Crea `.windsurf/rules/30-frozen-kits.md` con:
   - `trigger: glob` su `src/ui/idleVillage/frozen/**`, `src/ui/idleVillage/**/*.tsx`, `src/pages/minimal-*.tsx`.
   - Header: `# Frozen Kits Invariants`.
   - Requisiti che riflettano `src/ui/idleVillage/frozen/registry.ts` e `src/docs/docs/plans/systems_governance_alignment_plan.md` §4.A.1:
     - Ogni nuovo componente Idle Village deve essere aggiunto come kit frozen, estendere un kit esistente, o avere un'eccezione documentata in `src/docs/docs/idle_village/EXCEPTIONS.md`.
     - I consumer pubblici importano da `src/ui/idleVillage/frozen/kits/index.ts` (one-line barrel).
     - Ogni kit deve avere una catena `KitShell`/`withKitShell` per essere provider-safe.
     - I kit `certified` devono avere `contract`, `certManifestPath`, `docPath` e `hub` in `KIT_REGISTRY`.
     - I kit `draft` devono essere in `KIT_REGISTRY` con `status: 'draft'` e `docPath`.
     - Le nuove pagine minimal devono essere riflesse in `KIT_REGISTRY.hub` affinché `TestHub` resti generato.
     - `tests/contract/minimal-vs-test.spec.ts` deve passare per tutti i kit `certified`.
2. In `00-project-invariants.md` §"This layer is the shared baseline", aggiungi a l'esempi di sistemi a-priori obbligatori: **Frozen Kits** e **Documentation Governance** (mantenendo skin, i18n, telemetry, persistence, theming già elencati).
3. Verifica che i nuovi file Markdown rispettino lo stile esistente (frontmatter, header, elenchi).

OPERAZIONI VIETATE
- Non modificare `src/ui/idleVillage/frozen/registry.ts` o `TestHub.tsx` in questo task.
- Non creare nuovi kit o pagine.
- Non toccare `COMPONENT_MASTER_INDEX.md`.

ASSUNZIONI
- Il sistema Frozen Kits è implementato in `src/ui/idleVillage/frozen/**` e `scripts/freeze-kit.ts`.
- `TestHub` è generato da `KIT_REGISTRY.hub`.
- `tests/contract/minimal-vs-test.spec.ts` esiste già.
- GOV-001 e GOV-002 sono indipendenti e possono girare in parallelo.

REGRESSION SAFEGUARDS
TESTS TO RUN:
  - `npm run lint -- .windsurf/rules/`
  - `npm run build:check`
  - `npm run kanban:lint`
STOP CONDITIONS:
  - Qualsiasi errore di lint/build/kanban:lint deve essere corretto prima di chiudere.

AUTONOMIA & CHECK-IN
- Completa tutta la checklist prima di tornare.
- Non chiedere conferme salvo in caso di conflitti con `.windsurf/rules/` esistenti.

NOTE
- Questo è un task governance-only: non si tocca runtime.
- L'agente deve chiudere con la riga `KANBAN STATUS: GOV-001 – Completato (Evidence: test-results/gov-001-<YYYY-MM-DD>.log)`.
