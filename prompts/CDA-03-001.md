# CDA-03-001 — Material Engine (complexity/layers/binding)

## Context
Phase 03-001 of Config-Driven Architecture (CDA-MASTER-001). Implements the material engine with complexity scaling, layers, and binding.

## Objectives
- Create MaterialEngine
- Create complexityScaler
- Add unit tests for material engine
- Verify material engine integration

## Scope

### Files to Create
- `src/rendering/materials/MaterialEngine.ts` — Material engine
- `src/rendering/materials/complexityScaler.ts` — Complexity scaler
- `tests/unit/rendering/materials/MaterialEngine.test.ts` — Unit tests
- `tests/unit/rendering/materials/complexityScaler.test.ts` — Unit tests

### Files to Modify
- (None — standalone phase)

### Out of Scope
- Physics System (deferred to CDA-04-001)

## Guardrails

### Invariants
- **Config-first**: All material parameters in config
- **i18n**: i18n contracts per component
- **Skin contracts**: Skin contracts per component

### Constraints
- Material engine must be config-driven
- Complexity scaling must be tunable
- Tests must cover all material scenarios

## Implementation Plan

### Step 1: Create MaterialEngine
Create `MaterialEngine.ts` with:
- Config-driven material composition
- Layer system for materials
- Material binding logic

### Step 2: Create ComplexityScaler
Create `complexityScaler.ts` with:
- Config-driven complexity scaling
- Performance-based material adjustment
- Complexity validation

### Step 3: Add Unit Tests
Create comprehensive tests for material engine and complexity scaler

### Step 4: Verify Integration
- Ensure material engine works with rendering primitives
- Test with realistic material scenarios
- Validate config integration

## Safeguards

### Pre-Execution
- Verify `CDA-02-001` is marked as `Completato` in Kanban
- Run `npm run lint -- src/rendering/materials` (120s timeout)
- Run `npm run test -- tests/unit/rendering/materials/` (300s timeout)

### Post-Execution
- Run `npm run lint -- src/rendering/materials` (120s timeout)
- Run `npm run test -- tests/unit/rendering/materials/` (300s timeout)
- Run `npm run build:check` (180s timeout)
- Run `npm run kanban:lint` (30s timeout)

### Evidence Log
Create `test-results/cda-03-001-<date>.log` with:
- Lint results
- Test results (all tests passing)
- Build check output
- Kanban lint output
- Material engine summary

## Dependencies
- **blocked_by**: CDA-02-001 (Rendering Primitive System)

## Execution Hint
**architectural** — This task builds on rendering primitives and requires careful design of the material engine.

## Notes
- Config-first: all material parameters in config
- i18n contracts per component
- Skin contracts per component
