/**
 * NOTE: The runnable tests for the harness tool layer live in
 * `tests/unit/harness/tools.test.ts` (the vitest config only scans the
 * `tests/` and `src/` trees, not `scripts/`).
 *
 * This placeholder is intentionally empty. TODO: delete this file (Cascade
 * cannot delete files; remove manually).
 */

export {};
