# DISABLED — Idle Watcher Documentation

This documentation is legacy and has been archived at `archive/legacy_shutdown/README_WATCH.md`.

The shutdown/idle watcher is disabled. No component outside `scripts/shutdownManager/` mThe squeThe shutdown/idle watcher is disabled. No compontiv#!/bin/bash
echo "SHUTDOWN_CAPABILITY_NOTe"echo "SHUTl exit 0
