/**
 * minimal-destiny-astrolabe-v15 — la rotta di famiglia della v15.
 *
 * La pagina vive in `skill-check-v15.tsx` perche' non e' piu' un astrolabio:
 * e' la stella che copre la trama (desiderata v15). Qui c'e' solo l'alias, per
 * stare nella naviga insieme a tutte le altre versioni.
 */
export { default } from './skill-check-v15';
