/**
 * @fileoverview
 * Temporary shim preserved because legacy tooling still watches this path.
 * The real DragErrorOverlay RTL suite lives in
 * `tests/unit/idleVillage/DragErrorOverlay.test.tsx`.
 *
 * Keeping this file prevents ESLint/IDE lookups from failing during guardian
 * safeguard runs while the migration completes. Remove once all references are
 * updated to the new location.
 */

export {};
