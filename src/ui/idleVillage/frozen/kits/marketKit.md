# marketKit

**Status:** BLOCKED — canonical missing
- Canonical: `MarketActionCard` (`src/ui/idleVillage/map/actionCards/MarketActionCard.tsx` is a TODO stub)
- Next step: implement the canonical component, then re-bootstrap this kit.
