/**
 * pgcardKit.contract.test
 *
 * Contract test: ensures /minimal-pgcard renders the same subtree as the
 * reference route. See plan v2 §S5.
 */

import { describe, test } from 'vitest';

describe('pgcardKit contract', () => {
  test.todo('reference and minimal subtree are byte-identical after normalization');
});
