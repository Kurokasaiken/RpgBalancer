/**
 * pgcardKit.dom.test
 *
 * Snapshot test for the kit's rendered DOM with canonical fixture data.
 */

import { describe, test } from 'vitest';

describe('pgcardKit DOM snapshot', () => {
  test.todo('renders deterministically with canonical fixture');
});
