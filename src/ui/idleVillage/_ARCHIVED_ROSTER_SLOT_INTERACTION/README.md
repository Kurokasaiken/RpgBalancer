# Roster/Slot Interaction Archive

## Backup Date
2026-02-20

## Purpose
This directory contains 1:1 backup copies of all components involved in the roster/slot drag-and-drop interaction system before the final freeze and bug fixes.

## Structure (COMPLETE - 19 files)
```
components/     # Main UI components (7 files)
├── PgCard.tsx                    # Individual resident draggable card
├── CustomDragOverlay.tsx         # Drag overlay with offset handling
├── WorkerCard.tsx                # Visual resident card (compact mode)
├── DragContextStore.ts           # Global drag state management
├── DragContext.tsx               # Drag context provider
├── TestRosterPage.tsx            # Main test page with resident list
└── DropFeedbackUI.tsx            # Visual feedback for drop operations

slots/          # Slot system components (4 files)
├── residentSlotValidators.ts     # Validation logic for resident/slot matching
├── useResidentSlotController.ts  # Slot controller hook
├── types.ts                       # Type definitions
└── ResidentSlotRack.tsx          # Slot rack container

hooks/          # Supporting hooks (6 files)
├── useResidentDropValidation.ts  # Drop validation hook
├── useDragPreviewInstrumentation.ts # Drag preview tracking
├── useActiveHUDState.ts          # HUD state management
├── useActivityScheduler.ts       # Activity scheduling logic
├── useSandboxTiming.ts           # Sandbox timing bridge
└── useSandboxTimingBridge.tsx    # Timing bridge provider

supporting/     # Supporting components (3 files)
├── ActivitySlot.tsx              # Individual activity slot
├── ActiveHUD.tsx                 # Active state HUD display

tests/          # Test files (1 file)
└── test-route-drag-offset.spec.ts # E2E drag offset tests

docs/           # Documentation (2 files)
├── README.md                       # This file
└── COMPONENT_INVENTORY.md         # Complete component & data source inventory
```

## Key Features Preserved
- ✅ Drag offset handling (46px offset with 50px tolerance)
- ✅ Custom drag overlay with snapCenterToCursor modifier
- ✅ Drop validation and feedback system
- ✅ Resident slot assignment logic
- ✅ Telemetry and instrumentation
- ✅ Test coverage for drag interactions

## Known Limitations (as documented)
- Drag offset: ~46px constant offset due to size mismatch
- Playwright testing: Pointer events not generated by mouse.move()
- Portal rendering: getBoundingClientRect() timing issues

## Data Sources (Config-First Architecture)
- ✅ **Primary**: `src/balancing/config/idleVillage/` (game rules, stats, requirements)
- ✅ **Secondary**: Character Manager, Theme Presets, Engine Types
- ✅ **Complete Inventory**: See `COMPONENT_INVENTORY.md` for detailed data flow

## Migration Notes
When restoring or modifying these components:
1. Maintain the 50px tolerance in drag offset tests
2. Keep the custom modifier approach for overlay centering
3. Preserve the validation logic for resident/slot matching
4. Update documentation if changing interaction patterns
5. **CRITICAL**: Maintain config-first data source architecture

## Version
This backup represents the **complete system state** before final freeze.
- ✅ **19 components** fully backed up (1:1 copies)
- ✅ **All dependencies** identified and documented
- ✅ **Data sources** mapped (config-first architecture)
- ✅ **Components are stable** and tested with documented limitations
