import React from 'react';

export const StatWeigher: React.FC = () => {
    return (
        <div className="text-center p-8 text-gray-400">
            Stat Weigher is currently disabled due to engine migration.
        </div>
    );
};
