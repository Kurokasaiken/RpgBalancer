import React from 'react';

export const SpecularTester: React.FC = () => {
    return (
        <div className="text-center p-8 text-gray-400">
            Specular Tester is currently disabled due to engine migration.
        </div>
    );
};
