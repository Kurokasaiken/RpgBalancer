import React from 'react';

export const BalancingLab: React.FC = () => {
    return (
        <div className="text-center p-8 text-gray-400">
            Balancing Lab is currently disabled due to engine migration.
        </div>
    );
};
